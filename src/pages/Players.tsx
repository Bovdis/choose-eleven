import React, { FC } from 'react';
import PlayersList from '../components/PlayersList';

const Players: FC = () => {

  return (
    <div>
       <PlayersList />
    </div>
  );
}

export default Players;
